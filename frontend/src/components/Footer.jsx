function Footer() {
  return (
    <footer className="site-footer">
      <div>
        <strong>Factory Pattern Demo</strong>
        <p>Lazy singleton database connection with role-based user creation.</p>
      </div>
    </footer>
  )
}

export default Footer
